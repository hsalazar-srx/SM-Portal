# Production API base URL.
# The frontend and backend are served from the same IIS site,
# so this is a root-relative path — no hostname, no port.
# Must match the IIS sub-application path configured for the .NET backend.
VITE_API_URL=/api
